import { RainbowSimulatorDirective } from './rainbow-simulator.directive';

describe('RainbowSimulatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowSimulatorDirective();
    expect(directive).toBeTruthy();
  });
});
